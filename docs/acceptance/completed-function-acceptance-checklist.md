# BioAgent 新 Next.js 已完成功能验收检查单

## 1. 文档基线

- 检查日期：2026-07-23
- 主仓库基线：`67b2333`
- 正式前端：`packages/nextjs`
- 共享 UI：`@bioagent/chatui`
- 服务端默认地址：`http://39.106.18.219`
- 发布仓库：`agent_release`
- 推荐验收浏览器：Chrome 111+

本检查单只包含已经在正式 Next.js 中接入真实 Adapter/API，或已经形成可独立验证的工程能力。高保真原型中存在、但正式项目仍使用 Fixture、提示“待接入”或没有正式路由的功能，不计入已完成。

勾选含义：

- `[ ]`：等待本轮验收人员执行。
- `[x]`：已有代码、自动化或历史人工验收记录，不代表本轮环境已经重新执行。
- `副作用`：会修改真实账号、项目、成员、任务或文件数据，验收前应确认测试数据。

## 2. 验收前准备

- [ ] 服务端 `GET /health` 返回成功。
- [ ] 新前端可以访问，Docker 部署默认地址为 `http://<服务器IP>:3002`。
- [ ] 准备一个可正常登录并已加入实验室的测试账号。
- [ ] 测试账号至少有一个可写个人项目。
- [ ] 如需验收成员写操作，准备一个允许修改或移除的测试成员。
- [ ] 如需验收修改密码，确认测试账号密码允许变更，并准备好恢复方案。
- [ ] 浏览器开发者工具 Console 中没有与当前操作相关的错误。

## 3. 工程与 UI 系统

### UI-01 共享 UI 单一来源

- [x] 高保真原型、组件 Showcase 和正式 Next.js 使用同一个 `@bioagent/chatui`。
- [x] 通用组件、聊天组件、完整 Screen 和设计 Token 均由组件库输出。
- [x] 正式 Next.js 不复制同名组件源码，不深层导入组件库内部源码。
- [ ] 打开 `/showcase`，确认 Button、Input、Modal、Table、聊天组件和设计规范均可展示。
- [ ] 对照高保真原型 `/showcase`，抽查按钮、输入框、弹窗、圆角、边框、文字和阴影。
- [ ] 改变一个组件状态，确认交互行为与高保真原型一致。

预期结果：两个宿主消费同一组件实现，正式项目没有第二套主题或组件样式。

### UI-02 全局主题与样式规范

- [x] 颜色、阴影、滚动条和基础排版由 `--chatui-*` Token 统一提供。
- [x] Tailwind 使用语义颜色，不使用 `text-gray-*`、`bg-gray-*`、`border-gray-*` 等内置灰阶绕过规范。
- [x] 样式检查覆盖颜色字面量、内置色阶和原型边界。
- [ ] 抽查登录页、聊天页、项目页、任务页和设置页，确认不存在明显的浏览器默认黑边、默认阴影或第二套滚动条。

## 4. 认证与会话

### AUTH-01 登录

- [x] `/login` 已接入 `POST /api/auth/login`。
- [x] 支持 Cookie 会话、提交中禁用、重复提交保护和错误映射。
- [ ] 空邮箱或空密码时，登录按钮不可提交。
- [ ] 输入错误密码，显示“邮箱或密码不正确”，页面不跳转。
- [ ] 输入正确账号密码，成功进入 `/chat/new` 或安全的 `next` 回跳地址。
- [ ] 登录过程中连续点击按钮，不产生重复登录请求。
- [ ] 网络不可用时显示可理解的网络错误，不显示原始堆栈。

预期结果：登录成功后进入受保护工作区；失败时保留表单并显示明确错误。

### AUTH-02 会话恢复与路由守卫

- [x] `AuthProvider` 已接入 `GET /api/auth/me`。
- [x] 受保护路由未登录时会跳转 `/login?next=...`。
- [x] `next` 参数会拒绝外部地址、递归登录地址和歧义编码。
- [ ] 登录后刷新 `/chat/new`，仍保持登录态。
- [ ] 直接打开一个受保护地址，未登录时跳到登录页。
- [ ] 登录成功后返回原来的安全内部地址。
- [ ] 服务端返回 401 时清理前端会话并回到登录页。
- [ ] 服务端临时故障时显示“登录状态加载失败”和重试入口，而不是误判为未登录。

### AUTH-03 退出登录

- [x] 已接入 `POST /api/auth/logout`。
- [ ] 从用户菜单执行退出登录。
- [ ] 退出后访问受保护路由会回到登录页。
- [ ] 刷新页面后旧会话不会恢复。

### AUTH-04 修改密码（副作用）

- [x] `/system-settings` 已接入 `POST /api/auth/change-password`。
- [x] 支持当前密码错误、新旧密码相同、限流和服务端错误提示。
- [ ] 两次新密码不一致时不发请求。
- [ ] 当前密码错误时显示“当前密码不正确”。
- [ ] 使用正确当前密码修改成功后，用户被要求重新登录。
- [ ] 使用新密码可以登录，旧密码不能登录。

## 5. 工作区外壳与导航

### SHELL-01 受保护工作区

- [x] 聊天、项目、任务、成员和系统设置共用高保真 `AppShell`。
- [x] 页面切换保持同一工作区外壳，不进行浏览器整页刷新。
- [ ] 在“新对话、项目、自动化任务、成员、系统设置”之间切换，侧边栏不闪烁。
- [ ] 页面切换时浏览器 Network 不出现文档级重新加载。
- [ ] 当前菜单项、标题和内容区域与路由一致。

### SHELL-02 侧边栏展开与收起

- [x] 侧边栏宽度动画、内容不压缩和展开按钮已经修复。
- [ ] 收起侧边栏，内部内容保持原宽度并随容器裁切，不被横向压扁。
- [ ] 收起后显示高保真原型一致的展开按钮。
- [ ] 展开后按钮消失，侧边栏恢复原宽度。
- [ ] 动画层本身不出现滚动条。

### SHELL-03 历史对话

- [x] 已接入真实历史接口和会话详情接口。
- [x] 历史列表按服务端时间排序；本地发送后会将当前对话移动到最上方。
- [x] 点击历史对话会预加载详情并使用客户端导航，避免页面闪烁。
- [x] 历史消息首次呈现直接定位到底部，不使用可见的滚动动画。
- [ ] 打开任意历史对话，消息内容和附件信息正确恢复。
- [ ] 在旧对话发送新消息，该对话立即移动到“近期对话”顶部。
- [ ] 刷新后顺序仍与服务端 `updatedAt` 一致。
- [ ] 快速切换两个历史对话，页面不显示上一个对话的残留消息。

### SHELL-04 对话操作（删除为副作用）

- [x] 重命名、置顶/取消置顶、删除已连接真实会话接口。
- [x] 操作失败会刷新服务端状态，避免 UI 假成功。
- [ ] 重命名对话，刷新后名称保持。
- [ ] 置顶对话，刷新后置顶状态和顺序保持。
- [ ] 取消置顶，刷新后恢复普通排序。
- [ ] 删除测试对话，列表和当前缓存均移除。

说明：对话分享本期不实现，正式项目已隐藏入口；后续范围见 `CHAT-SCOPE-01`。

## 6. Chat 主链路

### CHAT-01 新建文本对话

- [x] `/chat/new` 已连接真实 SSE 聊天链路。
- [x] 支持 `meta`、`task_trace`、`text` 和 `error` 事件。
- [x] 创建会话后使用客户端路由进入 `/chat/[id]`。
- [ ] 输入普通文本并发送。
- [ ] 发送后立即显示用户消息、思考/搜索状态和助手流式内容。
- [ ] 新会话创建成功后 URL 变成真实会话 ID。
- [ ] 左侧近期对话出现新会话并位于顶部。
- [ ] 刷新页面后用户消息和助手回复可以恢复。

### CHAT-02 历史对话继续发送

- [x] `/chat/[id]` 已连接历史加载、继续发送和发送后对账。
- [ ] 打开已有对话继续发送一条消息。
- [ ] 流式回复完成后，不出现重复助手消息。
- [ ] 刷新后服务端持久化消息与发送完成时一致。
- [ ] 历史对话更新时间更新并移动到近期列表顶部。

### CHAT-03 停止、超时、断流与重新生成

- [x] 支持主动停止、连接超时、空闲超时和断流后的历史对账。
- [x] 支持对助手回复执行重新生成。
- [ ] 生成过程中点击停止，网络流被取消，输入区恢复可用。
- [ ] 停止后不会留下空白助手消息。
- [ ] 模拟传输失败，页面显示可重试错误，Toast/提示会按时消失。
- [ ] 点击重新生成，复用最近一条用户消息并产生新回复。
- [ ] 上游返回 503 时显示服务错误，不伪造成功回复。

### CHAT-04 富文本消息

- [x] 已支持 Markdown、GFM、代码高亮、KaTeX 和 Mermaid。
- [x] 代码块支持复制。
- [ ] 验收标题、列表、表格、引用、链接和任务列表。
- [ ] 验收至少一个代码块及复制按钮。
- [ ] 验收至少一个数学公式。
- [ ] 验收至少一个 Mermaid 图。
- [ ] 长内容、宽表格和代码块不会撑破聊天主区域。

### CHAT-05 附件上传与发送（副作用）

- [x] 已接入预签名、对象存储 PUT、附件登记和消息引用。
- [x] 多文件上传会沿用同一草稿 ID；后续文件失败时清理已登记文件。
- [x] 已覆盖上传中、成功、失败和删除状态。
- [ ] 选择允许的文件并发送，显示上传中状态。
- [ ] 上传成功后消息携带附件引用，助手可以消费附件上下文。
- [ ] 刷新对话后附件仍显示。
- [ ] 模拟对象存储失败，附件显示失败状态且消息不会伪装发送成功。
- [ ] 上传多个文件时，确认全部归属同一会话或草稿。

### CHAT-06 会话附件管理（删除为副作用）

- [x] 已接入附件列表、上下文启停和删除。
- [ ] 打开右侧附件面板，附件数量与服务端一致。
- [ ] 关闭某附件的上下文，刷新后状态保持。
- [ ] 重新启用上下文，刷新后状态保持。
- [ ] 删除测试附件，刷新后不再出现。
- [ ] 操作中按钮不可重复点击，失败时显示错误并保留真实状态。

### CHAT-07 Skill 与 Mira 引用

- [x] 输入区资源目录已接入 `/api/chat/resources`。
- [x] Skill 支持不可用原因，Mira 文档引用会转换为稳定上下文引用。
- [ ] 打开资源选择器，可以看到真实 Skill 列表。
- [ ] 不支持当前场景的 Skill 显示禁用原因且不可选。
- [ ] 选择一个可用 Skill 发送，服务端收到对应 Skill 标识。
- [ ] 选择一个 Mira 文档引用发送，助手可以消费该上下文。
- [ ] 刷新历史对话后 Skill 和 Mira 引用仍能恢复。

说明：项目文件和项目上下文尚未完成，见缺失单 `CHAT-GAP-02`。

### CHAT-08 助手消息基础操作

- [x] 助手回复支持复制。
- [x] 点赞/点踩具有当前页面内的交互状态。
- [ ] 复制助手回复，剪贴板内容与消息正文一致。
- [ ] 点赞、取消点赞、点踩、取消点踩的 UI 状态正确。

说明：点赞/点踩目前不持久化，不能作为已完成的数据功能，见缺失单 `CHAT-GAP-04`。

## 7. 项目

### PROJECT-01 项目列表

- [x] `/projects` 已接入 `/api/projects/bootstrap`。
- [x] 项目卡片使用真实项目数据和真实归属对话数量。
- [ ] 页面显示真实项目列表。
- [ ] 无项目时显示高保真空状态。
- [ ] 服务端失败时显示错误和重新加载入口。
- [ ] 点击项目进入 `/projects/[projectId]`。

### PROJECT-02 创建个人项目（副作用）

- [x] 已接入 `POST /api/projects`。
- [x] 项目名称、描述和基础表单校验已完成。
- [ ] 创建一个不带文档的个人项目。
- [ ] 创建成功后项目出现在列表顶部和侧边栏项目列表中。
- [ ] 刷新后项目仍存在。
- [ ] 尝试在创建时附带文档，系统明确提示暂不支持，而不是伪造上传成功。

### PROJECT-03 项目详情与编辑（副作用）

- [x] 已接入项目详情、名称修改和描述修改。
- [x] 文档摘要和项目对话从真实项目详情映射。
- [ ] 打开项目详情，名称、描述、成员数、知识条目和对话正确。
- [ ] 修改项目名称，刷新后保持。
- [ ] 修改项目描述，刷新后保持。
- [ ] 打开一个项目内对话，进入正确的 `/chat/[id]`。

### PROJECT-04 项目内创建对话（副作用）

- [x] 已接入带 `projectId` 的 Agent Session 创建。
- [x] `/chat/new` 的“工作项目”使用真实项目列表，首次发送会把所选 `projectId` 交给聊天接口。
- [ ] 在新对话页选择项目，按钮文案显示所选项目名称。
- [ ] 发送第一条消息后，会话归属所选项目且刷新后保持。
- [ ] 选择“不选择项目”后，新会话不绑定具体项目。
- [ ] 从项目详情创建新对话。
- [ ] 新对话出现在项目对话列表和左侧历史列表。
- [ ] 会话详情中的 `projectId` 与当前项目一致。

### PROJECT-05 项目成员管理（副作用）

- [x] 团队项目已接入成员目录、邀请、权限修改和移除接口。
- [x] 个人项目不会展示服务端不支持的团队管理入口。
- [ ] 团队项目管理员可以打开成员管理弹窗。
- [ ] 邀请一个测试成员，刷新后成员存在。
- [ ] 在“浏览/编辑”之间修改权限，刷新后保持。
- [ ] 移除测试成员，刷新后成员消失。
- [ ] 普通成员和个人项目用户看不到越权操作入口。

说明：项目文档详情、新建、上传和下载尚未完成，见缺失单 `PROJECT-GAP-01`。

## 8. 自动化任务

### TASK-01 任务列表

- [x] `/tools` 已接入 `GET /api/scheduled-tasks`。
- [x] 服务端状态、下一次运行时间和周期会转换为 UI ViewModel。
- [ ] 页面显示真实任务列表。
- [ ] 空列表显示高保真空状态。
- [ ] 加载失败显示重试入口。
- [ ] 刷新后任务状态与服务端一致。

### TASK-02 创建任务（副作用）

- [x] 已接入每天、每周和每月任务创建。
- [x] 已从正式编辑器移除“不重复”和“每小时”；每月限制 1–28 日。
- [ ] 创建每日任务并确认时间和时区正确。
- [ ] 创建每周任务并确认星期和时区正确。
- [ ] 创建每月任务并确认日期正确。
- [ ] 表单缺少名称或提示词时不提交。
- [ ] 创建成功后立即出现在列表，刷新后仍存在。

### TASK-03 编辑、暂停、恢复和删除（副作用）

- [x] 已接入任务编辑、暂停、恢复和删除。
- [ ] 分别编辑每天、每周和每月任务，刷新后周期和时间保持。
- [ ] 暂停活动任务，刷新后保持暂停。
- [ ] 恢复暂停任务，刷新后保持活动。
- [ ] 删除测试任务，刷新后不再出现。
- [ ] 运行中的任务不允许执行不安全的状态切换。

说明：“立即运行”和运行记录页面尚未完成；执行边界细化见缺失单 `TASK-GAP-01`、`TASK-GAP-02`。

## 9. 成员管理

### MEMBER-01 实验室成员列表

- [x] `/members` 已接入实验室详情、成员列表和真实角色。
- [x] 已区分管理员、本人和 PI 的可操作权限。
- [ ] 页面显示当前实验室名称、邀请码和成员列表。
- [ ] 当前用户、PI 和不可管理成员没有危险操作入口。
- [ ] 日期、头像、姓名、邮箱和角色正确。
- [ ] 没有活动实验室时显示明确提示。

### MEMBER-02 邀请码刷新（副作用）

- [x] 已接入邀请码重新生成接口。
- [ ] 管理员刷新邀请码。
- [ ] 页面立即显示新邀请码。
- [ ] 旧邀请码不再被当成当前邀请码展示。

### MEMBER-03 角色修改与移除（副作用）

- [x] Adapter 和 UI 已接入角色修改、成员移除。
- [ ] 将测试成员在“管理员/成员”之间切换，刷新后保持。
- [ ] 移除测试成员，刷新后不再出现。
- [ ] 本人和 PI 不允许被当前操作误删。

说明：成员“归属项目”没有服务端数据，页面明确显示“暂未提供”，见缺失单 `MEMBER-GAP-01`。

## 10. 系统设置

### SETTINGS-01 账户信息与头像（副作用）

- [x] `/system-settings` 已接入真实会话用户和头像上传。
- [x] 支持 PNG/JPG/WEBP，最大 2MB。
- [ ] 页面显示当前用户头像或文字兜底。
- [ ] 选择不支持的文件，前端阻止上传并显示明确错误。
- [ ] 上传有效头像，设置页和侧边栏同时更新。
- [ ] 更新过程不触发全屏会话加载。
- [ ] 刷新后头像仍然保持。

### SETTINGS-02 修改密码

参照 `AUTH-04` 执行，不重复修改真实测试账号。

## 11. 发布与运行

### RELEASE-01 发布仓库生成

- [x] `scripts/release-frontend.ps1` 会生成独立 `agent_release` 仓库内容。
- [x] 发布仓库包含 Next.js、ChatUI 构建产物、shared、Dockerfile 和部署脚本。
- [ ] 生成后的发布仓库工作区干净。
- [ ] `pnpm install --frozen-lockfile` 成功。
- [ ] `pnpm run release:check` 成功。

### RELEASE-02 Docker 一键部署

- [x] 发布仓库根目录提供无参数 `./deploy.sh`。
- [x] 使用当前 Git 提交号作为镜像标签。
- [x] 新容器通过健康检查后才删除旧容器；失败会回滚。
- [x] 本地验证过容器 `healthy` 且首页 HTTP 200。
- [ ] 在目标服务器执行 `git pull --ff-only && ./deploy.sh`。
- [ ] `docker ps` 显示 `bioagent-nextjs` 为运行中和健康状态。
- [ ] `curl http://127.0.0.1:3002/` 返回成功。
- [ ] 浏览器可以通过服务器地址访问。

## 12. 自动化回归

在主仓库根目录执行：

```bash
pnpm --filter @bioagent/chatui styles:check
pnpm --filter @bioagent/chatui typecheck
pnpm --filter @bioagent/nextjs test
pnpm --filter @bioagent/nextjs lint
pnpm --filter @bioagent/nextjs... build
```

验收标准：

- [x] Next.js 当前已有 78 项单元测试基线。
- [ ] 本轮所有测试通过，无失败、跳过或未处理异常。
- [ ] ChatUI 样式检查通过。
- [ ] ChatUI 类型检查通过。
- [ ] Next.js Lint 通过。
- [ ] Next.js production build 通过。

## 13. 验收结论

- 验收人：
- 验收环境：
- 前端提交：
- 服务端提交/版本：
- 验收日期：
- 结论：通过 / 有条件通过 / 不通过
- 阻塞问题：
- 非阻塞问题：
- 回归建议：
