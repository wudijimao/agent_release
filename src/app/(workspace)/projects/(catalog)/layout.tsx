import { ProjectCatalogLayout } from "./ProjectCatalogLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ProjectCatalogLayout>{children}</ProjectCatalogLayout>;
}
